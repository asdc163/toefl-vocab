# --- secrets: never commit these ---
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# --- dependencies / build ---
node_modules/
dist/
.vite/
*.tsbuildinfo

# --- source dictionary data (regenerate with npm run data:fetch) ---
# ecdict.csv is 63 MB; the built chunks in public/data/ are committed instead.
data/ecdict.csv
data/wordroot.txt
data/*.zip

# --- local / editor ---
.DS_Store
.vercel
*.log
